import { Box, Sphere, Plane, Circle, Cone, Cylinder, Torus, TorusKnot, Dodecahedron, Icosahedron } from "./index";

# Three.js Geometries

Three.js provides a variety of built-in geometries to create different shapes and structures in 3D space. Below is a comprehensive list of geometries, along with examples of how to use them in both Three.js and React-Three-Fiber.


## 1. BoxGeometry
Creates a rectangular box (cuboid) shape.

###### Three.js Example
```javascript
const geometry = new THREE.BoxGeometry(1, 1, 1); // width, height, depth
const material = new THREE.MeshBasicMaterial({ color: 0x00ff00 });
const cube = new THREE.Mesh(geometry, material);
scene.add(cube);
```

###### React-Three-Fiber Example
```jsx
function Box() {
  return (
    <mesh>
      <boxGeometry args={[1, 1, 1]} />
      <meshBasicMaterial color="green" />
    </mesh>
  );
}
```

<Box />

## 2. SphereGeometry
Creates a sphere.

###### Three.js Example
```javascript
const geometry = new THREE.SphereGeometry(1, 32, 32); // radius, widthSegments, heightSegments
const material = new THREE.MeshBasicMaterial({ color: 0xff0000 });
const sphere = new THREE.Mesh(geometry, material);
scene.add(sphere);
```

###### React-Three-Fiber Example
```jsx
function Sphere() {
  return (
    <mesh>
      <sphereGeometry args={[1, 32, 32]} />
      <meshBasicMaterial color="red" />
    </mesh>
  );
}
```

<Sphere />

## 3. PlaneGeometry
Creates a flat 2D plane.

###### Three.js Example
```javascript
const geometry = new THREE.PlaneGeometry(5, 5); // width, height
const material = new THREE.MeshBasicMaterial({ color: 0x0000ff, side: THREE.DoubleSide });
const plane = new THREE.Mesh(geometry, material);
scene.add(plane);
```

###### React-Three-Fiber Example
```jsx
function Plane() {
  return (
    <mesh>
      <planeGeometry args={[5, 5]} />
      <meshBasicMaterial color="blue" side={THREE.DoubleSide} />
    </mesh>
  );
}
```

<Plane />

## 4. CircleGeometry
Creates a circle.

###### Three.js Example
```javascript
const geometry = new THREE.CircleGeometry(1, 32); // radius, segments
const material = new THREE.MeshBasicMaterial({ color: 0xffff00 });
const circle = new THREE.Mesh(geometry, material);
scene.add(circle);
```

###### React-Three-Fiber Example
```jsx
function Circle() {
  return (
    <mesh>
      <circleGeometry args={[1, 32]} />
      <meshBasicMaterial color="yellow" />
    </mesh>
  );
}
```

<Circle />


## 5. ConeGeometry
Creates a cone or pyramid shape.

###### Three.js Example
```javascript
const geometry = new THREE.ConeGeometry(1, 2, 32); // radius, height, radialSegments
const material = new THREE.MeshBasicMaterial({ color: 0xff00ff });
const cone = new THREE.Mesh(geometry, material);
scene.add(cone);
```

###### React-Three-Fiber Example
```jsx
function Cone() {
  return (
    <mesh>
      <coneGeometry args={[1, 2, 32]} />
      <meshBasicMaterial color="magenta" />
    </mesh>
  );
}
```

<Cone />

## 6. CylinderGeometry
Creates a cylinder.

###### Three.js Example
```javascript
const geometry = new THREE.CylinderGeometry(1, 1, 2, 32); // topRadius, bottomRadius, height, radialSegments
const material = new THREE.MeshBasicMaterial({ color: 0x00ffff });
const cylinder = new THREE.Mesh(geometry, material);
scene.add(cylinder);
```

###### React-Three-Fiber Example
```jsx
function Cylinder() {
  return (
    <mesh>
      <cylinderGeometry args={[1, 1, 2, 32]} />
      <meshBasicMaterial color="cyan" />
    </mesh>
  );
}
```

<Cylinder />

## 7. TorusGeometry
Creates a torus (ring or donut shape).

###### Three.js Example
```javascript
const geometry = new THREE.TorusGeometry(1, 0.4, 16, 100); // radius, tube, radialSegments, tubularSegments
const material = new THREE.MeshBasicMaterial({ color: 0xff8800 });
const torus = new THREE.Mesh(geometry, material);
scene.add(torus);
```

###### React-Three-Fiber Example
```jsx
function Torus() {
  return (
    <mesh>
      <torusGeometry args={[1, 0.4, 16, 100]} />
      <meshBasicMaterial color="orange" />
    </mesh>
  );
}
```

<Torus />

## 8. TorusKnotGeometry
Creates a torus knot.

###### Three.js Example
```javascript
const geometry = new THREE.TorusKnotGeometry(1, 0.3, 100, 16); // radius, tube, tubularSegments, radialSegments
const material = new THREE.MeshBasicMaterial({ color: 0xaa00ff });
const torusKnot = new THREE.Mesh(geometry, material);
scene.add(torusKnot);
```

###### React-Three-Fiber Example
```jsx
function TorusKnot() {
  return (
    <mesh>
      <torusKnotGeometry args={[1, 0.3, 100, 16]} />
      <meshBasicMaterial color="purple" />
    </mesh>
  );
}
```

<TorusKnot />


## 9. DodecahedronGeometry
Creates a dodecahedron (12-faced polyhedron).

###### Three.js Example
```javascript
const geometry = new THREE.DodecahedronGeometry(1); // radius
const material = new THREE.MeshBasicMaterial({ color: 0x00aa00 });
const dodecahedron = new THREE.Mesh(geometry, material);
scene.add(dodecahedron);
```

###### React-Three-Fiber Example
```jsx
function Dodecahedron() {
  return (
    <mesh>
      <dodecahedronGeometry args={[1]} />
      <meshBasicMaterial color="green" />
    </mesh>
  );
}
```

<Dodecahedron />

## 10. IcosahedronGeometry
Creates an icosahedron (20-faced polyhedron).

###### Three.js Example
```javascript
const geometry = new THREE.IcosahedronGeometry(1); // radius
const material = new THREE.MeshBasicMaterial({ color: 0x0000aa });
const icosahedron = new THREE.Mesh(geometry, material);
scene.add(icosahedron);
```

###### React-Three-Fiber Example
```jsx
function Icosahedron() {
  return (
    <mesh>
      <icosahedronGeometry args={[1]} />
      <meshBasicMaterial color="blue" />
    </mesh>
  );
}
```

<Icosahedron />
